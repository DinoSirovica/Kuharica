# Example backend environment variables for Kuharica
# Copy this file to .env and fill the values (do NOT commit .env)

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database (optional - if you prefer env config)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=kuharica

# Server
PORT=8081

